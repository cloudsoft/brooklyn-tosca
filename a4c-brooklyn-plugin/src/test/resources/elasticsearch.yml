
index:
  number_of_shards: 1
  number_of_replicas: 0
path:
  data: target/alien/elasticsearch/data
  work: target/alien/elasticsearch/work
  logs: target/alien/elasticsearch/logs
  conf: /src/test/resources

gateway:
  recover_after_nodes: 1
  expected_nodes: 1

discovery:
  zen:
    ping:
      multicast:
        enabled: false
      unicast:
        enabled: true
        hosts: localhost